program sample;
{ ------------------------------------------------------------ }
var
tcase, a, b: Int;
c: Char = 'a';
r: Real;
bl: Bool = True;
{ 我们的程序中string必须初始化 }
s: String = 'Hello';	{ only support single quote }
array1: array[5] of char;
array2: array[2] of bool;

{ ------------------------------ }
{ 返回值暂时只支持int, real, bool, char }

{ ------------------------------ }
BEGIN 
{ a := Int(c) + Int(num); }

r := 1.0
{ bl := gcd(a, b) + -5 }
end.