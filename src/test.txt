program sample;
{ ------------------------------------------------------------ }
const
PI = 3.1415926;
var
r: real;
label
x;

{ ------------------------------ }
{ 返回值暂时只支持int, real, bool, char }

{ ------------------------------ }
BEGIN 
{ a := Int(c) + Int(num); }

x: r := 5.0*real((1--4))+PI
{ bl := gcd(a, b) + -5 }
end.