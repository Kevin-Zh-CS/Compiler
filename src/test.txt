program sample;
{ ------------------------------------------------------------ }
var
a : int;
r : real;

{ ------------------------------ }
function gcd(a: Int; b: Int): Int;	{ header }
{ local declaration }
var
array1: array[2] of int;
function my_mod(a: Int; b: Int): Int;
begin
	my_mod := a + b
end;
{ local declaration end }
begin
    array1[1] := 2;
{    if true then}
	gcd := my_mod(a, b)*array1[1]	{ * no semicolon }
end;
{ ------------------------------ }

BEGIN 
r := 1.0
{ a := gcd(1, 2) }
{ array1[0] := 'a'; }
{ bl := gcd(a, b) + -5 }
end.