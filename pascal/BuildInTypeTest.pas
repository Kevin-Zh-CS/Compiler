program BuildInTypeTest;
const
	ic = 1;
	rc = 1.5;
	bc = false;
	cc = 'a';
var
	iv : int;
	rv : real;
	bv : bool;
	cv : char;

begin
    iv := 1;
    rv := 99.9;
    bv := true;
    cv := 'a';
    writeln(ic);
    writeln(rc);
    writeln(bc);
    writeln(cc);
    writeln(iv);
    writeln(rv);
    writeln(bv);
    writeln(cv)
end
.
